import User from ".user.model.js";
import mongoose from "mongoose";

const tripSchema=new mongoose.Schema({
    username:{

    },

    source:{
        type:string,
        reqired:true,
    },

    destination:{
        type:string,
        required:true,
    },

    startdate:{
        type:Date,
        require:true,

    },
    enddate:{
        type:Date,
        require:true,
    }

    totaldays:{
        type:Number,
        required:true;

    },

    budget:{
        type:Number,
        required:true;

    },

    totalpersons:{
        type:Number,
        required:true;

    },

    intrests:{
        type:array,
        

    },

},{
    timestamps:true,
});